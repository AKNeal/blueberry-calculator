# /public/recipes/

Drop real recipe photography here using the naming convention:

```
{slug}-hero.jpg
{slug}-ingredients.jpg
```

The `{slug}` matches the recipe entry in `lib/recipes.ts`. See
`/public/placeholders/README.md` for the full image spec and the swap-in
workflow.
